═══════════════════════════════════════════════════════════════════
  KOMENDY DO PROMOCJI UŻYTKOWNIKA NA SUPER_ADMIN
═══════════════════════════════════════════════════════════════════

📧 Email do promocji: admin@kadryhr.local

─────────────────────────────────────────────────────────────────────
METODA 1: Uniwersalny Skrypt (NAJLEPSZA - ZALECANA) ⭐
─────────────────────────────────────────────────────────────────────

cd /home/deploy/apps/kadryhr-app/backend
node scripts/manageRoles.js promote admin@kadryhr.local

# Inne przydatne komendy:
node scripts/manageRoles.js list                    # Lista adminów
node scripts/manageRoles.js check admin@kadryhr.local  # Sprawdź rolę
node scripts/manageRoles.js help                    # Pomoc

─────────────────────────────────────────────────────────────────────
METODA 2: Prosty Node.js Script
─────────────────────────────────────────────────────────────────────

cd /home/deploy/apps/kadryhr-app/backend
node scripts/promoteToSuperAdmin.js admin@kadryhr.local

─────────────────────────────────────────────────────────────────────
METODA 2: Bash Script z MongoDB Shell
─────────────────────────────────────────────────────────────────────

cd /home/deploy/apps/kadryhr-app/backend
./scripts/promote-admin.sh admin@kadryhr.local

─────────────────────────────────────────────────────────────────────
METODA 3: Bezpośrednie polecenie MongoDB (ONE-LINER)
─────────────────────────────────────────────────────────────────────

mongosh mongodb://localhost:27017/kadryhr --quiet --eval "db.users.updateOne({email:'admin@kadryhr.local'},{\$set:{role:'super_admin'}}); db.users.findOne({email:'admin@kadryhr.local'},{name:1,email:1,role:1})"

─────────────────────────────────────────────────────────────────────
METODA 4: MongoDB Shell Interaktywnie
─────────────────────────────────────────────────────────────────────

mongosh mongodb://localhost:27017/kadryhr

# Następnie w MongoDB shell:
use kadryhr
db.users.updateOne(
  { email: "admin@kadryhr.local" },
  { $set: { role: "super_admin" } }
)

# Sprawdź wynik:
db.users.findOne(
  { email: "admin@kadryhr.local" },
  { name: 1, email: 1, role: 1 }
)

exit

─────────────────────────────────────────────────────────────────────
WERYFIKACJA PO ZMIANIE
─────────────────────────────────────────────────────────────────────

# Sprawdź czy zmiana się powiodła:
mongosh mongodb://localhost:27017/kadryhr --quiet --eval "db.users.findOne({email:'admin@kadryhr.local'},{name:1,email:1,role:1})"

# Powinno zwrócić:
# {
#   _id: ObjectId("..."),
#   name: "Admin User",
#   email: "admin@kadryhr.local",
#   role: "super_admin"
# }

─────────────────────────────────────────────────────────────────────
UWAGI
─────────────────────────────────────────────────────────────────────

1. Upewnij się, że MongoDB działa:
   sudo systemctl status mongod

2. Jeśli MongoDB wymaga autoryzacji, dodaj credentials:
   mongosh mongodb://username:password@localhost:27017/kadryhr

3. Po zmianie roli, użytkownik musi się wylogować i zalogować ponownie

4. Super admin może:
   ✅ Inicjalizować uprawnienia (przycisk "Inicjalizuj uprawnienia")
   ✅ Zarządzać uprawnieniami wszystkich użytkowników
   ✅ Edytować uprawnienia innych super adminów
   ✅ Dostęp do wszystkich modułów bez dodatkowych uprawnień

═══════════════════════════════════════════════════════════════════
