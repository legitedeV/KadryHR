{
  "name": "kadryhr",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8.15.5",
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev",
    "format": "pnpm prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}